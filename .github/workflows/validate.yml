name: Validate dotfiles

on:
  pull_request:
  push:
    branches: [master]

jobs:
  validate:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: brew install jq shellcheck gitleaks chezmoi
      - run: bash scripts/validate.sh
