packages:
  mise:
    # azure-cli resolves a prerelease in its dependency tree, so the pipx
    # backend needs the flag. Without it mise adds it to the deployed config
    # itself, which then reads as a local modification on every apply.
    azure-cli:
      version: "2.89.1"
      uvx_args: "--prerelease=allow"
    bat: "0.26.1"
    bun: "1.3.14"
    fd: "10.4.2"
    fzf: "0.74.2"
    gh: "2.97.0"
    gitleaks: "8.30.1"
    go: "1.26.6"
    herdr: "0.8.0"
    kubectl: "1.36.3"
    neovim: "0.12.4"
    node: "26.7.0"
    # azure-cli resolves to the pipx backend, which needs Python 3.10 or newer.
    # The macOS system Python is 3.9, so pin an interpreter rather than
    # depending on whatever the host happens to ship.
    python: "3.13.15"
    ripgrep: "15.2.0"
    shellcheck: "0.11.0"
    starship: "1.26.0"
    stylua: "2.5.2"
    tealdeer: "1.8.1"
    uv: "0.12.4"

  mise_server:
    "cargo:tree-sitter-cli": "0.26.12"
    caddy: "2.11.4"
    rust: "1.97.1"

  npm:
    "@opencode-ai/cli": "0.0.0-beta-18050"
    tree-sitter-cli: "0.26.12"

  macos:
    taps:
      - asmvik/formulae
    trusted_taps:
      - asmvik/formulae
    brews:
      - age
      - asciiquarium
      - bash
      - chezmoi
      - cmake
      - cowsay
      - git
      - git-filter-repo
      - git-lfs
      - gnupg
      - jq
      - lolcat
      - mise
      - ponysay
      - tree
      - wget
      - asmvik/formulae/skhd
      - asmvik/formulae/yabai
    casks:
      - 1password
      - 1password-cli
      - docker-desktop
      - font-hack-nerd-font
      - font-jetbrains-mono
      - ghostty
      - google-chrome
      - granola
      - handy
      - linear
      - microsoft-teams
      - obsidian
      - slack
      - tailscale-app
      - wispr-flow
      - zoom
    enterprise_casks:
      - font-hack-nerd-font
      - font-jetbrains-mono
      - ghostty

  amazon_linux:
    - alsa-lib
    - at-spi2-atk
    - atk
    - bash
    - clang
    - cmake
    - cronie
    - cups-libs
    - docker
    - gcc
    - gcc-c++
    - git
    - git-lfs
    - gzip
    - jq
    - gtk3
    - libatomic
    - libX11
    - libXcomposite
    - libXdamage
    - libXext
    - libXfixes
    - libXrandr
    - make
    - mesa-libgbm
    - nss
    - pango
    - tar
    - tree
    - unzip
    - util-linux-user
    - wget
    - zsh
